"use client";

import { useState, useTransition } from "react";
import { Rnd } from "react-rnd";
import { saveVoucherTemplateLayoutAction } from "@/features/admin/actions/voucher-template-actions";
import { browserTopToPdfBottom, pdfBottomToBrowserTop, type VoucherTemplateLayoutV1 } from "@/features/vouchers/lib/voucher-template-layout";

const SCALE = 3;
const areas = ["valueArea", "serviceArea", "validityArea", "codeArea", "qrArea"] as const;
type AreaKey = (typeof areas)[number];
const labels: Record<AreaKey, string> = { valueArea: "VALUE", serviceArea: "SERVICE", validityArea: "VALIDITY", codeArea: "CODE", qrArea: "QR" };

export function VoucherTemplateLayoutEditor({ templateId, initialLayout, previewSrc }: { templateId: string; initialLayout: VoucherTemplateLayoutV1; previewSrc?: string }) {
  const [layout, setLayout] = useState(initialLayout);
  const [selected, setSelected] = useState<AreaKey>("valueArea");
  const [pending, startTransition] = useTransition();
  const area = layout[selected];
  const textArea = selected === "qrArea" ? null : layout[selected];
  const update = (key: AreaKey, patch: Record<string, unknown>) => setLayout((current) => ({ ...current, [key]: { ...current[key], ...patch } } as VoucherTemplateLayoutV1));
  return <section className="rounded-2xl border border-white/10 bg-white/5 p-5"><div className="grid gap-5 xl:grid-cols-[1fr_18rem]"><div><div className="relative mx-auto max-w-[648px] overflow-hidden border border-white/40 bg-neutral-900" style={{ aspectRatio: "216 / 105" }}>{previewSrc ? <img src={previewSrc} alt="" aria-hidden="true" draggable={false} className="pointer-events-none absolute inset-0 h-full w-full select-none object-contain" /> : null}<div className="absolute inset-0 z-10"><div className="absolute border border-dashed border-amber-300/60" style={{ left: 3 * SCALE, bottom: 3 * SCALE, width: 210 * SCALE, height: 99 * SCALE }} /><div className="absolute left-1/2 top-0 h-full border-l border-dashed border-white/20" /><div className="absolute top-1/2 left-0 w-full border-t border-dashed border-white/20" />{areas.map((key) => { const item = layout[key]; const baseline = "baselineMm" in item ? item.baselineMm : null; return <Rnd key={key} bounds="parent" size={{ width: item.widthMm * SCALE, height: item.heightMm * SCALE }} position={{ x: item.xMm * SCALE, y: pdfBottomToBrowserTop(item.yMm, item.heightMm) * SCALE }} lockAspectRatio={key === "qrArea"} dragGrid={[SCALE / 2, SCALE / 2]} resizeGrid={[SCALE / 2, SCALE / 2]} onClick={() => setSelected(key)} onDragStop={(_, data) => update(key, { xMm: data.x / SCALE, yMm: browserTopToPdfBottom(data.y / SCALE, item.heightMm) })} onResizeStop={(_, __, ref, ___, pos) => { const widthMm = ref.offsetWidth / SCALE; const heightMm = ref.offsetHeight / SCALE; update(key, { xMm: pos.x / SCALE, yMm: browserTopToPdfBottom(pos.y / SCALE, heightMm), widthMm, heightMm }); }} className={`border ${selected === key ? "border-amber-300 bg-amber-300/20" : "border-cyan-200/70 bg-cyan-200/10"}`}><span className="pointer-events-none p-1 text-[10px] font-bold text-white">{labels[key]}</span>{baseline === null ? null : <span className="pointer-events-none absolute left-0 right-0 border-t border-red-300" style={{ top: `${(item.heightMm - (baseline - item.yMm)) / item.heightMm * 100}%` }} />}</Rnd>; })}</div></div><p className="mt-2 text-xs text-white/55">Snap 0,5 mm · PDF souřadnice od levého spodního rohu · červená linka je baseline.</p></div><aside className="space-y-3"><label className="block text-sm">Oblast<select value={selected} onChange={(event) => setSelected(event.target.value as AreaKey)} className="mt-1 w-full rounded bg-black/30 p-2">{areas.map((key) => <option key={key} value={key}>{labels[key]}</option>)}</select></label>{(["xMm", "yMm", "widthMm", "heightMm"] as const).map((field) => <label key={field} className="block text-sm">{field} (mm)<input type="number" step="0.5" value={area[field]} onChange={(event) => update(selected, { [field]: Number(event.target.value) })} className="mt-1 w-full rounded bg-black/30 p-2" /></label>)}{textArea ? <><label className="block text-sm">Baseline (mm)<input type="number" step="0.5" value={textArea.baselineMm} onChange={(event) => update(selected, { baselineMm: Number(event.target.value) })} className="mt-1 w-full rounded bg-black/30 p-2" /></label><label className="block text-sm">Max. řádků<input type="number" min="1" value={textArea.maxLines} onChange={(event) => update(selected, { maxLines: Number(event.target.value) })} className="mt-1 w-full rounded bg-black/30 p-2" /></label></> : null}<button disabled={pending} onClick={() => startTransition(() => saveVoucherTemplateLayoutAction(templateId, layout))} className="w-full rounded-xl bg-[var(--color-accent)] px-4 py-2 font-semibold text-black disabled:opacity-50">{pending ? "Ukládám…" : "Uložit draft"}</button></aside></div></section>;
}
